%h1 All Videos
- @curators.each do |curator|
  %h2= curator.name
  - curator.videos.each do |video|
    .row-fluid
      %iframe{:frameborder => "0", :src => "http://www.youtube.com/embed/Q3Ya1AMHwiM?feature=player_detailpage"} 
      = link_to video.title, video_path(video)
      = video.caption
      = link_to 'Delete', video, method: :delete, data: { confirm: 'Are you sure?' }
