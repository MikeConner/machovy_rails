.span12
  - ads.each do |ad|
    .span3
      - if ad.banner?
        = raw ad.destination
      - else
        .thumbnail
          = link_to image_tag(ad.teaser_image_url(:product_front_page).to_s), ad.destination, :target => '_blank' 
